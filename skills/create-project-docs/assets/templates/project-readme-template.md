<div align="center">

<h1><Project Name></h1>

**<one-sentence value statement from repo evidence>**

<!-- Optional evidence-backed badges. Remove this block if the repo has no factual badges. -->
Status badge from repo evidence
Runtime or language badge from repo evidence
Primary interface badge from repo evidence

</div>

## Quick Install

Prerequisites:

- `<tool or runtime from repo evidence>`
- `<tool or runtime from repo evidence>`

Bootstrap the environment:

```bash
<canonical setup commands from repo evidence>
```

## Quick Start

Run the shortest evidence-backed path to first success:

```bash
<canonical first-run commands from repo evidence>
```

Explain what this run produces, validates, or proves in one short paragraph.

## What <Project Name> Does

Explain the repository's top-level purpose in plain language first. Then summarize the major concerns or subsystems with short bullets.

- `<top-level concern or package>`
- `<top-level concern or package>`
- `<top-level concern or package>`

## Core Concepts

- `<core noun>`: `<plain-English explanation>`
- `<core noun>`: `<plain-English explanation>`
- `<core noun>`: `<plain-English explanation>`

## Go Deeper

- `docs/references/<topic>.md`: `<what this deeper doc answers>`
- `docs/references/<topic>.md`: `<what this deeper doc answers>`

<!-- Move content to docs/references when it needs multiple examples, tables, caveats, or schema detail. -->
